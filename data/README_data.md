# Data folder

Suggested layout:

- `data/raw/` : original CSVs (not committed)
- `data/processed/` : OWL/Turtle/TSV triples (not committed)
- `data/sample/` : small sample files you are allowed to share

If you need to share large files, consider Git LFS.
